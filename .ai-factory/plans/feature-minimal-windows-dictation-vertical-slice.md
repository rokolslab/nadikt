# План реализации: Minimal Windows Dictation Vertical Slice

Branch: feature/minimal-windows-dictation-vertical-slice
Created: 2026-08-05

## Original Request
minimal Windows dictation vertical slice with local ASR and safe insertion

## Settings
- Testing: yes
- Logging: verbose
- Docs: yes

## Roadmap Linkage
Milestone: "Ранний Windows vertical slice"
Rationale: План проверяет минимальный путь Windows dictation через local ASR и safe insertion до полноценного desktop UI.

## Research Context
Source: .ai-factory/RESEARCH.md (Active Summary, Updated: 2026-08-03 10:08, SHA256: 893b79d40fa6b54ffe6a5ded5d75814a9c03d4b9388a0f37e1027ac1b2df750f)

Topic: ASR model delivery and coding-dictation benchmark direction for Nadikt MVP.

Goal: Preserve fully local/private runtime while allowing practical model delivery options for installation and prioritizing accurate Russian dictation with coding-related English terms.

Constraints:
- Runtime dictation, ASR, insertion, dictionary, and normalization must work without internet after installation.
- Internet during installation is allowed only as fallback if embedded installer, separate model pack, or removable media delivery is not practical.
- Runtime must accept only a local verified model package path with checksum/package validation; Hub model names or implicit network downloads remain forbidden.
- Separate model pack files next to the installer or copied from a flash drive are acceptable.
- Installer/model pack size is not currently a major constraint.
- Manual model pack selection/installation by the user is acceptable for MVP.
- ASR priority is Russian accuracy with coding anglicisms, library names, commands, identifiers, abbreviations, and technical terms.
- Full mixed Russian-English dictation can be postponed or marked experimental after the first usable MVP.

Decisions:
- Separate install-time delivery from runtime package validation.
- Support multiple delivery channels for the same local model package format: embedded installer, separate offline package, and online installer fallback.
- Reframe early MVP ASR target from generic `Russian` vs full `Russian + English` to `Russian with coding terms` as the practical priority.
- Do not choose final ASR model until real local package load/probe and quality/resource benchmark results exist.

Open questions:
- Which concrete model packages can be legally redistributed or downloaded by the installer.
- Whether GigaAM cache-style local package loading works with real `.ckpt` and tokenizer files under blocked-network conditions.
- Whether faster-whisper CTranslate2 local packages provide enough Russian/coding-term quality on CPU INT8.
- What minimal coding-dictation dataset best represents the user's real workflow.
- Which Windows packaging mechanism will support model pack import/download with checksum verification.

Success signals:
- Real local package probe loads, warms up, short-transcribes, and closes at least one candidate under externally blocked network.
- Missing/corrupted package fails before backend import/load and never attempts network access.
- First coding-dictation benchmark covers Russian phrases with code terms such as `pytest`, `docker compose`, `React component`, `FastAPI route`, `access token`, `pull request`, and common identifiers.
- Benchmark compares quality, RTF, latency, CPU, and peak RAM on the current i3 12th gen development machine before later Windows baseline validation.
- A decision matrix can recommend one MVP ASR path and one fallback path without weakening runtime privacy/offline guarantees.

Next step: Create a full AI Factory plan for `ASR model delivery and coding dictation benchmark`, including documentation updates, dataset expansion, real model package preparation outside Git, offline load gates, and first quality/resource benchmark.

## Commit Plan
- **Commit 1** (after tasks 1-4): `feat: add dictation pipeline contracts and capture`
- **Commit 2** (after tasks 5-8): `feat: wire local asr dictation pipeline`
- **Commit 3** (after tasks 9-12): `feat: add safe windows insertion harness`
- **Commit 4** (after tasks 13-15): `test: validate windows dictation slice`

## Tasks

### Phase 1: Core contracts and state
- [x] Task 1: Добавить production-модель состояния dictation session и удержания результата в `src/nadikt/domain/dictation/session.py` и `src/nadikt/domain/dictation/__init__.py`. Ожидаемое поведение: явные переходы для `idle`, `capturing`, `recognizing`, `normalizing`, `inserting`, `completed`, `cancelled` и `failed`; запрет concurrent sessions и двойной вставки; удержание финального или частичного transcript после обработанных ошибок до успешной delivery или отмены пользователем. Logging requirements: domain objects не логируют сами; наружу доступны только безопасные state names, session IDs, segment counts и outcome codes для application logging; запрещено раскрывать transcript text, audio paths, clipboard data, window titles, handles, PIDs и process names. Dependency notes: основа для tasks 5, 8, 9, 12 и 13.

- [x] Task 2: Добавить минимальные production ports в `src/nadikt/domain/ports/audio.py` и `src/nadikt/domain/ports/insertion.py`. Ожидаемое поведение: определить bounded one-shot audio capture contract, который возвращает SDK-neutral segment metadata, совместимую с `AsrSegmentInput`, а также safe insertion contracts для target capture/revalidation, clipboard transaction и input dispatch с opaque DTO и typed outcome codes. `TargetToken` должен быть только random opaque key; HWND/PID/thread ID/COM/UIA objects остаются внутри adapter-owned store, token invalidated после terminal outcome/cancel, а stale/PID-reused/changed UIA runtime identity должны fail closed. Logging requirements: каждый DTO `repr` должен редактировать audio paths, text payload, clipboard formats с private data, target internals, HWND/PID/thread IDs, file names и process/window names; DEBUG logs разрешены только для safe operation names, durations, counts и outcome codes. Dependency notes: следует стилю `src/nadikt/domain/ports/asr.py` и нужен до application/infrastructure adapters.

- [x] Task 3: Зафиксировать минимальный dependency gate для Windows slice в `pyproject.toml`, `docs/research/windows_insertion_spike_results.md` или отдельной docs note, если выбор требует объяснения. Ожидаемое поведение: выбрать минимальный проверяемый путь для audio capture, VAD/segmentation scope и UI Automation provider перед написанием adapters; не утверждать production library stack без локального smoke/prototype; внести только необходимые optional/runtime dependencies или явно оставить adapter behind opt-in import; зафиксировать licensing/packaging/offline impact. Logging requirements: не добавлять logging в dependency metadata; docs/prototype output не должен включать audio paths, transcript samples, clipboard payloads, private model paths, window titles, process names или local user paths. Dependency notes: нужен до tasks 4, 10 и 11; `.ai-factory/DESCRIPTION.md` прямо говорит, что audio/VAD/insertion libraries ещё не утверждены.

- [x] Task 4: Добавить controlled Windows audio capture adapter в `src/nadikt/infrastructure/audio/windows_capture.py` и package init files под `src/nadikt/infrastructure/audio/`. Ожидаемое поведение: реализовать one-shot bounded microphone capture для выбранного/default input device, минимальную device enumeration/selection, PCM format suitable for ASR, pre-buffer target, level measurement, cancellation, disconnect/permission failure handling и cleanup temp audio; long dictation/VAD remains out-of-scope for this slice, но segment metadata должна включать `segmentation_policy_id` и не полагаться на `faster-whisper vad_filter` как production VAD. Logging requirements: DEBUG-log для device capability category, selected-device opaque ID/hash, sample rate/channel count, duration, level status, cancellation/failure codes и temp cleanup outcome; никогда не логировать audio path, raw samples, transcript, device user-friendly names, OS account names или private file paths. Dependency notes: зависит от tasks 2 и 3; требуется task 5 и task 12, иначе harness не сможет выполнить real bounded recording.

- [x] Task 5: Создать application orchestration service в `src/nadikt/application/services/dictation_pipeline.py` и package init files под `src/nadikt/application/`. Ожидаемое поведение: внедрять через constructor `AudioCapturePort`, `AsrEngine`, text normalizer и insertion service; выполнять одну bounded session `capture -> transcribe_segment -> normalize -> insert`; явно пометить long dictation/VAD/segment assembly как out-of-scope для этого slice; обрабатывать cancel/failure без завершения процесса; возвращать safe outcome DTO со статусом retained result, но без текста в `repr`. Logging requirements: DEBUG-log для lifecycle transitions, dependency boundary calls, segment duration, backend/package IDs из `safe_engine_log_context`, timing events и typed failures; никогда не логировать transcript text, audio paths, insertion text, clipboard payload, target/window/process details или raw SDK errors. Dependency notes: зависит от tasks 1, 2 и 4; до real Windows adapters использовать fakes в тестах.

### Phase 2: Local ASR and deterministic text path
- [x] Task 6: Добавить runtime model-package validation boundary под `src/nadikt/infrastructure/model_packages/` без импорта benchmark modules из application runtime. Ожидаемое поведение: принимать не raw package path из CLI/config, а binding из trusted inventory/sidecar для `package_id + candidate_id + backend`; отклонять example manifests, Hub names, URLs, traversal/absolute unsafe paths, self-declared package roots, placeholder checksums и unapproved local evaluation; проверять sidecar digest, critical file size/SHA-256/role, backend/package format, Nadikt/backend compatibility, GigaAM required files и controlled ffmpeg/native tool asset или fail closed; падать до ASR SDK import/load. Logging requirements: DEBUG-log для validation phases, package ID, candidate ID, backend, manifest version, checksum prefixes и failure codes; никогда не логировать absolute user paths, raw manifest payloads с private paths, model file names вне allowlisted roles или environment secrets. Dependency notes: требуется для task 7 и должен сохранять offline-by-construction policy; schemas alone are not enough because critical Python validator rules live outside JSON Schema.

- [x] Task 7: Подключить один local ASR candidate к pipeline через существующие `AsrEngine` adapters в `src/nadikt/infrastructure/asr/` и небольшой composition helper в `src/nadikt/bootstrap.py`. Ожидаемое поведение: выбирать candidate только explicit `candidate_id` + validated package binding; не делать auto fallback/ranking и не объявлять final ASR winner; загружать verified local package через `AsrLoadOptions`, выполнять warm up на bounded non-scored segment, транскрибировать ровно один captured segment, закрывать ресурсы при completion/failure; GigaAM может быть temporary working candidate, faster-whisper fallback/baseline, публичный статус remains `NOT DECIDED`. Logging requirements: DEBUG-log для safe ASR lifecycle timing, package/candidate/backend IDs, readiness state, segment IDs и typed failures; не логировать transcript text, audio paths, SDK stack payloads, Hub names как accepted config или model package absolute paths. Dependency notes: зависит от tasks 5 и 6; real local model checks остаются opt-in и вне Git.

- [x] Task 8: Добавить минимальную deterministic text normalization в `src/nadikt/domain/text/normalization.py` и `src/nadikt/domain/text/__init__.py`. Ожидаемое поведение: нормализовать только safe deterministic whitespace/newline boundaries и final insertion text shape, нужные slice; не переносить benchmark coding-term mappings из `benchmarks/asr/coding_term_normalization.py` в production; оставить user dictionary и voice commands на последующие работы, если tests не потребуют небольшой generic rule type. Logging requirements: domain normalizer не логирует сам; application может DEBUG-log только normalizer rule IDs, input/output character counts и outcome codes; запрещено логировать recognized text или replacement dictionary content. Dependency notes: используется task 5 перед insertion и тестами в task 13.

### Phase 3: Safe Windows insertion boundary
- [x] Task 9: Создать production insertion orchestration в `src/nadikt/application/services/insertion_service.py`. Ожидаемое поведение: выполнять one-shot delivery с captured target token, target revalidation перед mutation, clipboard prepare/commit/restore policy, optional direct Unicode fallback только при явном permit, без automatic retry, с retained result при failure и typed fail-closed outcomes: `target_changed`, `target_unavailable`, `target_protected`, `target_elevated`, `clipboard_unsafe`, `dispatch_unconfirmed`, `pending_clipboard_restore`; target token cleanup после terminal outcome/cancel; explicit `restore_original`/`discard_original`; не перезаписывать external clipboard sequence change; retained snapshot remains retryable при restore failure. Logging requirements: DEBUG-log для service phases, opaque token generation/correlation ID, clipboard snapshot format counts, dispatch method, modifier preflight result, token cleanup и outcome codes; никогда не логировать insertion text, clipboard contents, HWND/PID/thread ID, window titles, process names, filenames, control contents или raw Windows API errors с private details. Dependency notes: зависит от tasks 1 и 2; использовать `experiments/windows_insertion` только как design evidence, не как import source.

- [x] Task 10: Добавить UI Automation target-safety provider в `src/nadikt/infrastructure/platform/windows/uia.py` или внутри `src/nadikt/infrastructure/platform/windows/target.py` с явной границей. Ожидаемое поведение: реализовать stable UIA runtime identity для focused control, `IsPassword`/protected-field detection, unknown-provider fail-closed outcome, PID reuse/change detection через adapter-internal store, и contract tests для `safe`, `target_protected`, `target_unavailable`, changed UIA runtime ID и stale token; positive real-app insertion разрешён только если UIA protection probe известен и false. Logging requirements: DEBUG-log только provider availability, safe boolean flags, normalized outcome codes и token correlation; запрещено логировать UIA element names, control contents, automation IDs with user data, window titles, process names, handles, PIDs или COM object repr. Dependency notes: зависит от tasks 2, 3 и 9; без этого browser/Word/password safety не может считаться accepted.

- [x] Task 11: Добавить initial Windows platform adapters в `src/nadikt/infrastructure/platform/windows/target.py`, `src/nadikt/infrastructure/platform/windows/clipboard.py` и `src/nadikt/infrastructure/platform/windows/input.py`. Ожидаемое поведение: реализовать narrow contracts из task 2 для controlled Windows host; fail closed, когда target identity, protection state, integrity/elevation, clipboard restorability, delayed/unknown formats или modifier state неопределённы; preserve spike learnings, но переписать production adapters с opaque DTOs и без зависимости domain/application от Windows APIs; static/import-boundary tests должны доказать, что `src/nadikt` и production tests не импортируют `experiments.windows_insertion` или `insertion_spike`. Logging requirements: DEBUG-log для adapter phase names, safe capability flags, format type allowlist results by category/count, sequence-race outcome и dispatch cleanup state; никогда не логировать target internals, clipboard payloads, request text, handles, PIDs, process/window names, filenames или private control metadata. Dependency notes: зависит от tasks 9 и 10; Notepad может быть первым positive target, но Word/browser/1C без доказанной UIA safety остаются fail-closed blockers, а не pass.

### Phase 4: Harness, tests, and documentation
- [x] Task 12: Добавить controlled Windows vertical-slice harness в `src/nadikt/presentation/cli/windows_dictation_slice.py` и composition wiring в `src/nadikt/bootstrap.py`. Ожидаемое поведение: operator запускает одну bounded recording, harness захватывает target до dictation, выполняет local ASR из verified local package, нормализует текст, пытается safe insertion в тот же target, печатает только safe status/outcome fields и завершается без tray/overlay/global-hotkey UI; если outcome содержит `pending_clipboard_restore` или retained original snapshot, harness обязан предложить explicit `restore_original`/`discard_original` и не завершаться молча до operator decision. Logging requirements: DEBUG-log для command options с redacted paths, lifecycle state, selected candidate ID, safe timing, pending-restore state и final outcome; stdout/stderr никогда не должны включать transcript text, audio paths, insertion text, clipboard data, window/process names, handles или private local package paths. Dependency notes: зависит от tasks 5, 7, 8, 9, 10 и 11; global hotkey и PySide6 desktop shell вне scope этого minimal slice.

- [ ] Task 13: Добавить automated tests для slice. Files: `tests/unit/test_dictation_session.py`, `tests/unit/test_dictation_pipeline.py`, `tests/unit/test_insertion_service.py`, `tests/contract/test_audio_port_contract.py`, `tests/contract/test_insertion_port_contract.py`, `tests/integration/test_runtime_model_package_validation.py` и Windows-only opt-in tests под `tests/windows/`. Ожидаемое поведение: fake-backed tests покрывают state transitions, one-shot pipeline ordering, cancellation, retained result after insertion failure, pending clipboard restore/discard flow, fail-before-SDK-import/load package validation with import traps for `gigaam`/`faster_whisper`, insertion fail-closed outcomes, no retry/double insert, token cleanup/stale token rejection, static no-import boundary from `src/nadikt` to `experiments.windows_insertion`, privacy-safe `repr` и отсутствие canary в logs/stdout/stderr; opt-in real asset tests skip unless enabled but fail when enabled config/assets are incomplete. Logging requirements: test fixtures должны проверять, что DEBUG logs содержат safe state/outcome/timing fields и не содержат canary transcript, audio path, clipboard, model path, target/window/process, handle, PID или filename payloads. Dependency notes: зависит от tasks 1-12; real ASR assets и Windows host checks остаются opt-in через environment/config вне Git.

- [ ] Task 14: Добавить versioned manual Windows acceptance matrix для slice в `docs/testing.md` или `docs/research/windows_dictation_slice_acceptance.md`. Ожидаемое поведение: matrix rows включают Notepad normal, classic `EDIT`/`ES_PASSWORD`, Edge/Chrome/Firefox normal и password fields, Word isolated document, 1C if installed, elevated target, Windows 10, target-changed same window/control, destroyed target, real clipboard text/Unicode/image/file-list restoration, unknown/HTML/RTF/delayed-rendered reject, external sequence race, default mic unavailable, short utterance, silence/no empty insert, pre-buffer/first-word behavior и disconnect handling; `NOT RUN` remains blocker, not pass; private ASR paths/assets are never recorded. Logging requirements: acceptance notes must record only safe outcome codes, durations, target category, OS/app category and blocker status; no transcript, audio path, clipboard contents, window titles, process names, filenames, handles, PIDs, local model paths or user names. Dependency notes: depends on tasks 4, 10, 11, 12 and 13; separates manual acceptance evidence from automated tests.

- [ ] Task 15: Выполнить mandatory docs checkpoint через `/aif-docs` для реализованного slice. Вероятно затронутые files: `docs/getting-started.md`, `docs/testing.md`, `docs/research/windows_insertion_spike_results.md`, `docs/research/local_asr_offline_package_prototype.md`, `docs/research/local_asr_performance_benchmark_results.md` и `AGENTS.md`, если entry points или структура materially changed. Ожидаемое поведение: documented harness command, dependency decisions, offline/local-package requirements, Windows opt-in acceptance procedure, safe insertion limitations, privacy logging constraints, explicit out-of-scope items such as desktop shell/global hotkey/full dictionary/long VAD/final ASR model decision, and ASR docs reconciliation: private lifecycle may be noted only as private/non-publishable while publication remains blocked by `offline_evidence.status=NOT VERIFIED`; не переписывать disposable spike results as MVP acceptance, только добавить supersession/link note if applicable. Logging requirements: документация должна описывать safe log fields и forbidden payloads, но не включать sample transcripts, real local paths, clipboard contents, window titles, process names или private model package locations. Dependency notes: зависит от фактического implementation behavior и выполняется до marking plan done, потому что `Docs: yes`.
